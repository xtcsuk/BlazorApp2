@using AntDesign

@if (_data == null)
{
    return;
}

<AutoComplete 
    @ref="@_autoComplete"
    @bind-Value="@value" 
    Options="@_data" 
    OnSelectionChange="OnChangeAsync" 
    OnInput="OnInputChangeAsync"
    Placeholder="@PlaceholderCaption"
/>
